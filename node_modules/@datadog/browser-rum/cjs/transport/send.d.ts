import type { EndpointBuilder } from '@datadog/browser-core';
import type { BrowserSegmentMetadata } from '../types';
export declare function send(endpointBuilder: EndpointBuilder, data: Uint8Array, metadata: BrowserSegmentMetadata, rawSegmentBytesCount: number): void;
export declare function toFormEntries(input: object, onEntry: (key: string, value: string) => void, prefix?: string): void;
